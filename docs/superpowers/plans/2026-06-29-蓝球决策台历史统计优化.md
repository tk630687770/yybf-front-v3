# 蓝球决策台历史统计优化

创建日期：2026-06-29

## 目标

优化“同状态历史完成统计”的信息密度和可读性，让窗口状态、完成状态、年度分布和排序操作更清晰。

## 范围

- 仅修改前端 `src/views/BlueDecisionConsole.vue`。
- 不修改后端接口。
- 不改变保存样本、复盘、评分逻辑。

## 任务清单

| 编号 | 状态 | 任务 |
|---|---|---|
| BH-T1 | 已完成 | 顶部窗口标签显示窗口名称和当前状态两行文本 |
| BH-T2 | 已完成 | “全部”模式移除窗口卡片内 `当前：状态` 文本 |
| BH-T3 | 已完成 | “年度/全期”改为紧凑按钮组，放在窗口标题右侧 |
| BH-T4 | 已完成 | 独立窗口模式卡片标题只显示完成状态，右侧显示 `全X/今Y` |
| BH-T5 | 已完成 | 独立窗口模式年度表支持年份和次数排序，默认年份升序，首次点击升序 |
| BH-T6 | 已完成 | 年度摘要改为数字格子：次数去重升序，中位数背景高亮，今年次数文字高亮，旁边只显示平均值数字 |
| BH-T7 | 已完成 | 固定排序箭头占位，避免切换排序时列宽抖动 |
| BH-T8 | 已完成 | 压缩独立窗口状态卡最小宽度，提高一行容纳数量 |

## 断点

当前断点：开发完成，等待提交。

## 验证

- 已运行 `npm run build`，通过；仅有 Vite chunk size 提示。
- 已用浏览器自动化打开 `/blue-decision-console`，输入 `2026073` 并点击“读取窗口”验证：
  - 全部模式仍显示三窗口。
  - 全部模式卡片内不再出现 `当前：`。
  - 顶部窗口标签显示 3 个状态副标题。
  - 独立窗口模式显示 3 个完成状态卡。
  - 独立窗口标题只显示完成状态，例如 `8*_6_2-`。
  - 独立窗口年度表有 6 个可排序表头，点击“次数”后首次显示升序箭头 `▲`。
  - 年度摘要数字格子已渲染。
