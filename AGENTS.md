# yybf-front-v3 前端协作规则

## 开工必读

当任务涉及页面结构、预测展示、快照复盘、诊断研究、窗口同步、IndexedDB、本地缓存或后端接口联动时，先阅读：

1. `README.md`
2. `docs/frontend-development-workflow.md`
3. `docs/frontend-module-plan.md`
4. `docs/project-structure.md`

如果任务涉及某次已制定的方案或阶段性改造，还需要阅读 `docs/superpowers/specs/` 和 `docs/superpowers/plans/` 下最近相关的设计与计划文件。

## 不确定先确认

如果用户的指令、页面职责、接口语义、数据来源、是否落库、是否影响正式预测或复盘真实性存在无法确认的地方，不要直接推进实现。先用中文向用户提出具体问题，确认后再继续。

需要确认的典型情况：

- 同一个词可能指向不同区域，例如“候选池”可能指主推荐候选、9+1复式池、10注6+1票面池或诊断扩展池。
- 某个诊断结果应该只是展示、进入回测，还是影响正式预测输出。
- 某个按钮应该触发后端重新计算、读取已保存快照，还是只切换前端显示状态。
- 修改可能影响历史快照复盘真实性。

## 页面职责

当前前端主页面按职责分为六个入口：

- `/window-console`：基础窗口操作台，负责窗口基础数据、本地窗口矩阵、人工观察、模拟选择和窗口状态同步。
- `/prediction-console`：实时预测台，负责开奖前查看主推荐、9+1复式、10注6+1单式并保存预测快照。
- `/snapshot-review`：快照复盘台，负责开奖后读取开奖前快照、复盘、保存诊断包。
- `/diagnostic-lab`：诊断研究台，负责多期趋势、漏号分布、贝叶斯冷热、保底扩展和配额回测等研究诊断。
- `/historical-replay-lab`：历史回放实验台，负责最终票面与收益口径的严格前视批量实验。
- `/entry-recall-lab`：红球入口召回研究台，负责第一层入口池的组件、组合、TopN召回、逐球证据和稳定性研究。

不要把复盘、诊断、预测和窗口操作重新堆回同一个页面。如果确实需要跨页面展示同一信息，优先抽公共组件或公共 composable。

## 文档更新纪律

涉及以下变化时，需要同步更新 `docs/` 下对应文档：

- 新增、删除或重命名页面入口。
- 修改页面职责边界。
- 新增预测、复盘、诊断或同步接口。
- 改变快照保存、读取、复盘或诊断包保存流程。
- 改变模型版本、展示口径、正式预测输出或回测/诊断解释。
- 新增阶段性方案、计划、验证结论或重要取舍。

不要只把关键结论留在聊天记录里。

## 验证纪律

前端代码提交或推送前至少执行：

```bash
npm run build
```

涉及页面交互、路由或视觉结构时，还需要在浏览器中验证相关页面是否能正常打开、按钮是否符合预期、控制台是否没有前端错误。

## 完成后提交推送纪律

无论本次修改属于前端、后端联动、文档还是研究记录，只要开发和验证已经完成，就需要在结束前执行：

1. 检查 `git status`，确认只包含本次任务相关改动。
2. 根据实际修改内容编写清晰的提交消息，避免使用含糊的 `update`、`fix`。
3. 提交本次改动。
4. 推送到 GitHub 远端分支。

如果工作区存在用户未说明来源的无关改动，不要混入本次提交；需要先向用户说明并确认处理方式。
